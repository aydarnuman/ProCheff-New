# ProCheff AI Environment Variables
# Bu dosya gerekli ortam değişkenlerinin bir örneğidir
# .env.local dosyanızı oluşturmak için bu dosyayı kullanın

# =============================================================================
# GEREKLI (REQUIRED) - Bu değişkenler olmadan uygulama çalışmayacaktır
# =============================================================================

# Anthropic Claude API Key (ZORUNLU)
# https://console.anthropic.com/ adresinden API key alabilirsiniz
ANTHROPIC_API_KEY=sk-ant-api03-your-api-key-here

# =============================================================================
# İSTEĞE BAĞLI (OPTIONAL) - Bu değişkenler olmadan da uygulama çalışır
# =============================================================================

# OpenAI API Key (İsteğe bağlı)
# https://platform.openai.com/ adresinden API key alabilirsiniz
# OPENAI_API_KEY=sk-your-openai-api-key-here

# NextAuth Secret (Authentication için)
# Rastgele bir string oluşturun: openssl rand -base64 32
# NEXTAUTH_SECRET=your-nextauth-secret-here

# Database URL (Veritabanı bağlantısı için)
# DB_URL_SECRET=postgresql://user:password@localhost:5432/procheff

# =============================================================================
# SISTEM AYARLARI (SYSTEM SETTINGS)
# =============================================================================

# Node Environment (development, test, production)
NODE_ENV=development

# Server Port (Varsayılan: 8080)
PORT=8080

# =============================================================================
# GELİŞTİRME ORTAMI AYARLARI (DEVELOPMENT SETTINGS)
# =============================================================================

# Git SHA (Otomatik olarak CI/CD tarafından eklenir)
# GIT_SHA=dev

# =============================================================================
# NOTLAR
# =============================================================================
# 1. Bu dosyayı .env.local olarak kopyalayın
# 2. Gerçek API anahtarlarınızı girin
# 3. .env.local dosyasını asla Git'e commit etmeyin (zaten .gitignore'da)
# 4. Production ortamında ortam değişkenlerini hosting sağlayıcınızda ayarlayın
