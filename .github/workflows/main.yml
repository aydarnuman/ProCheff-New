name: deploy
on:
  push:
    branches: [ "main" ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    env:
      PROJECT_ID: degsan-site
      REGION: us-west1
      SERVICE: procheff
      REPO: procheff
      IMAGE: us-west1-docker.pkg.dev/degsan-site/procheff/procheff:${{ github.sha }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install & Build
        run: |
          npm ci
          npm run type-check
          npm run build

      # GCP Authentication temporarily disabled - requires manual setup
      # To enable: Add GCP_WIF_PROVIDER and GCP_DEPLOY_SA secrets to repository
      # - name: Auth to Google Cloud
      #   uses: google-github-actions/auth@v2
      #   with:
      #     workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
      #     service_account: ${{ secrets.GCP_DEPLOY_SA }}

      - name: Build Success
        run: |
          echo "✅ Build completed successfully!"
          echo "📦 TypeScript checks passed"
          echo "🚀 Ready for deployment"
          echo "📍 Manual deploy: gcloud run deploy procheff --source . --region us-west1"

      # Temporarily disabled deployment steps
      # - name: Setup gcloud
      #   uses: google-github-actions/setup-gcloud@v2

      # Temporarily disabled deployment steps
      # - name: Setup gcloud
      #   uses: google-github-actions/setup-gcloud@v2

      # - name: Configure Docker auth
      #   run: |
      #     gcloud auth configure-docker ${REGION}-docker.pkg.dev --quiet

      # - name: Build & Push Image
      #   run: |
      #     docker build -f docker/Dockerfile -t $IMAGE .
      #     docker push $IMAGE

      # - name: Deploy to Cloud Run
      #   run: |
      #     gcloud run deploy $SERVICE \
      #       --project $PROJECT_ID \
      #       --region $REGION \
      #       --image $IMAGE \
      #       --platform managed \
      #       --allow-unauthenticated \
      #       --concurrency 80 \
      #       --cpu 1 \
      #       --memory 512Mi \
      #       --port 8080 \
      #       --set-env-vars "GIT_SHA=${{ github.sha }}" \
      #       --set-secrets "ANTHROPIC_API_KEY=ANTHROPIC_API_KEY:3" \
      #       --set-secrets "OPENAI_API_KEY=OPENAI_API_KEY:1" \
      #       --set-secrets "NEXTAUTH_SECRET=NEXTAUTH_SECRET:1" \
      #       --set-secrets "DB_URL_SECRET=DB_URL_SECRET:1"

      # - name: Smoke - Health
      #   run: |
      #     URL=$(gcloud run services describe $SERVICE --project $PROJECT_ID --region $REGION --format='value(status.url)')
      #     curl -fsSL "$URL/api/health" | jq .
